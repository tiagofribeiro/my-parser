import { Box, TextField } from "@mui/material";

const HomePage = () => {
    return (
        <Box sx={{ bgcolor: "background.paper" }}>
            <TextField />
        </Box>
    );
}

export default HomePage;